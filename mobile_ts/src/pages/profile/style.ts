import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  profileText: {
    fontSize: 25,
    padding: 15,
    color: '#DC143C',
  },
  restTextPassword: {
    marginTop: 10,
    color: '#DC143C',
  },
});
